package main

func main() {

	//sliice
	// var books []string

	// // approach 1
	// // add items to append

	// books = append(books, "data scince")
	// books = append(books, "algorithm" , "digital marketting", "full stack" )
	// // for range
	// // for idx, vel := range books {
	// // 	// fmt.Println( "index", idx, "value", )
	// // }
	// for i len:=(books) 0; i < count; i++ {
	// 	fmt.println(books[i])

	// }

	// slicing / selecting certan items inside slice

	number := []in{12, 13, 14, 15, 16}

	sliced := numbers[1:4]
	sliced2 := numbers[1:]
	sliced3 := numbers[:4]
	fmt.println(sliced)
	fmt.println(sliced2)
	fmt.println(sliced3)

	// calculate the sum of the numbers
	var scores []int = []int{66,67,78,97,89,90}
	var sum int = 0
	for scores := range; scores {
		sum += socre


	}
	fmt.Println(sum)

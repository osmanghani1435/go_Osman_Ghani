package main

import "fmt"

func merge() {

	fmt.Println(spinSlice([]int{1, 2, 3, 4, 5}))
	fmt.Println(spinSlice([]int{6, 7, 8}))
	fmt.Println(spinSlice([]int{8, 5, 4, 3, 2, 1}))

}
func spinSlice(numbers []int) []int {

	result = append(result, numbers[0])
	result = append(result, numbers[len(numbers)-1])

	numbers = numbers[1 : len(numbers)-1]
	
	for idx, idx, i < len{numbers}; i++ {
		
		
	}

	return result
}
